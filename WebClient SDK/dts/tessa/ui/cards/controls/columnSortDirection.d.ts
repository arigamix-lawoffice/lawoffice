export declare enum ColumnSortDirection {
    None = 0,
    /**
     * Sorts in ascending order.
     * @type {Number}
     */
    Ascending = 1,
    /**
     * Sorts in descending order.
     * @type {Number}
     */
    Descending = 2
}
