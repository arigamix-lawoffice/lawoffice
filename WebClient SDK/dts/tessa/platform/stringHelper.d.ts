/**
 * Provides helper methods for working with the string object.
 */
export declare const StringHelper: {
    /**
     * Returns a value indicating whether the string being tested is null, undefined, an empty string, or contains only spaces.
     * @param value The string to test.
     * @returns True if {@link value} is null, undefined, is an empty string, or consists only of whitespaces.
     */
    isNullOrWhiteSpace: (value: string | null | undefined) => boolean;
};
