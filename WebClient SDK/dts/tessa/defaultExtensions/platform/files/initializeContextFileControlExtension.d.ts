import { FileControlExtension, IFileControlExtensionContext } from 'tessa/ui/files';
/**
 * Расширение, инициализирующее состояние сортировок, группировок и фильтраций по умолчанию.
 */
export declare class InitializeContextFileControlExtension extends FileControlExtension {
    openingMenu(context: IFileControlExtensionContext): void;
}
