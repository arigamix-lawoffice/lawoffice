import { ICardUIExtensionContext } from 'tessa/ui/cards';
/**
    /// Вспомогательные методы для UI расширений правил расчёта ACL и генераторов умных ролей.
    */
export declare class AclUIHelper {
    /**
     * Метод для инициализации представления с ошибками.
     * @param context Контекст UI расширений.
     */
    static initializeErrorsView(context: ICardUIExtensionContext): void;
}
